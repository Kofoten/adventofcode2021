{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build"
                ]
            },
            "args": [ "11", "2" ],
            "cwd": "${workspaceFolder}",
        }
    ]
}