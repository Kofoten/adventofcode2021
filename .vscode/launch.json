{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build"
                ]
            },
            "args": [ "14", "1" ],
            "cwd": "${workspaceFolder}",
        }
    ]
}